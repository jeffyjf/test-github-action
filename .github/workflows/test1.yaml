name: test workflow

on:
  release:
    types:
      - "published"

jobs:
  test_release_published:
    runs-on: ubuntu-20.04
    steps:
      - name: Dump trigger event
        run: echo '${{ toJSON(github.event) }}'
